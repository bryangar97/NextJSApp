export default function NotFound(){
    return(
        <main className="not-found">
            <h1>Meal Not Found</h1>
            <p>We can not found the requested Meal.</p>
        </main>
    )
}